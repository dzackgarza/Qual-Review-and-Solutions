\subsection{Exercises}

\begin{problem}[Hungerford 5.3.5]
\label{prob:1.1}
Show that if $f\in K[x]$ has degree $n$ and $F$ is a splitting field of $f$ over $K$, the $[F: K]$ divides $n!$.
\end{problem}

\begin{problem}[Hungerford 5.3.12]
\label{prob:1.1}
Let $E$ be an intermediate field extension in $K \leq E \leq F$.
\begin{enumerate}
    \item Show that if $u\in F$ is separable over over $K$, then $u$ is separable over $E$.
    \item Show that if $F$ is separable over $K$, then $F$ is separable over $E$ and $E$ is separable over $K$.
\end{enumerate}
\end{problem}

\begin{problem}[Hungerford 5.3.13]
\label{prob:1.1}
Show that if $[F: K] < \infty$, then the following conditions are equivalent:
\begin{enumerate}
    \item $F$ is Galois over $K$
    \item $F$ is separable over $K$ and $F$ is a splitting field of some polynomial $f\in K[x]$.
    \item $F$ is a splitting field over $K$ of some polynomial $f\in K[x]$ whose irreducible factors are separable.
\end{enumerate}
\end{problem}

\begin{problem}[Hungerford 5.4.1]
\label{prob:1.1}
Suppose that $f\in K[x]$ splits in $F$ as
$$
f = \prod_{i=1}^k (x-u_i)^{n_i}
$$
with the $u_i$ distinct and each $n_i \geq 1$. Let 
$$
g(x) = \prod_{i=1}^k (x-u_i) = \sum_{i=1}^k v_i x^i
$$

and let $E  = K(\{v_i\}_{i=1}^k)$. Then show that the following hold:
\begin{enumerate}
    \item $F$ is a splitting field of $g$ over $E$.
    \item $F$ is Galois over $E$.
    \item $\mathrm{Aut}_E(F) = \mathrm{Aut}_K(F)$.
\end{enumerate}
\end{problem}

\begin{problem}[Hungerford 5.4.10 a/g/h]
Determine the Galois groups of
the following polynomials over the corresponding fields:
\begin{enumerate}
    \item $x^4- 5$ over $\mathbb Q, \mathbb Q(\sqrt 5), \mathbb Q(i\sqrt 5)$.
    \item $x^3 - 2$ over $\mathbb Q$.
    \item $(x^3-2)(x^2-5)$ over $\mathbb Q$.
\end{enumerate}
\label{prob:1.1}

\end{problem}

\begin{problem}[Hungerford 5.6.11]
\label{prob:1.1}
If $f \in K[x]$ is irreducible of degree $m > 0$ and $\mathrm{char}(K)$ does not divide $m$, then $f$ is separable.
\end{problem}

\hline

% Qual Problems
\newpage
\subsection{Qual Problems}

\begin{problem}
\label{prob:1.1}
Let $E/F$ be a Galois field extension, and let $K/F$ be an intermediate field of $E/F$. Show that $K$ is normal over $F$ iff $\mathrm{Gal}(E/K) \trianglelefteq \mathrm{Gal}(E/F)$.
\end{problem}

\begin{problem}
\label{prob:1.1}
Let $F \subset L$ be fields such that $L/F$ is a Galois field extension with Galois group equal to $D_8 = \left< \sigma,\tau \mid \sigma^4 = \tau^2 = 1,~ \sigma\tau = \tau \sigma^3 \right>$. Show that there are fields $F \subset E \subset K \subset L$ such that $E/F$ and $K/E$ are Galois field extensions, but $K/F$ is not Galois.
\end{problem}

\begin{problem}
\label{prob:1.1}
Let $f(x) = x^3 - 7$.
\begin{enumerate}
    \item Let $K$ be the splitting field for $f$ over $\mathbb Q$. Describe the Galois group of $K / \mathbb Q$ and the intermediate fields between $\mathbb Q$ and $K$. Which intermediate fields are not Galois over $\mathbb Q$?
    
    \item Let $L$ be the splitting field for $f$ over $\mathbb R$. What is the Galois group $L/ \mathbb R$?
    
    \item Let $M$ be the splitting field for $f$ over $\mathbb F_{13}$, the field with 13 elements. What is the Galois group of $M / \mathbb F_{13}$?
\end{enumerate}
\end{problem}
